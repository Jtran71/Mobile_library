Mobile Library

Mobile Library is an Android application that lets users browse, manage, and track books through a simple, mobile-first interface. The app is built natively in Java and uses Firebase as its backend database for real-time data storage and syncing.

Features
Browse and search a catalog of books
Add, edit, and delete book entries
Real-time data sync powered by Firebase
User-friendly, mobile-optimized interface
